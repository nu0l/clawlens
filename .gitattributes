# Ensure consistent line endings across platforms
* text=auto

# Go source files must use LF to pass gofmt
*.go text eol=lf

# Shell scripts must use LF
*.sh text eol=lf

# Makefile must use LF
Makefile text eol=lf
